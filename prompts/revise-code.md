Revise the existing HTML and CSS based on the diagnosis.
Treat the output as a single web section, not a full page.
Prefer minimal necessary changes.
Do not rewrite the whole structure unless required.
Preserve valid HTML/CSS.
Output the full updated HTML and full updated CSS.
Improve visual fidelity to the source screenshot in this priority order:
1) element position and alignment
2) spacing and sizing
3) typography (font size/weight/line-height)
4) colors and contrast
Do not introduce new decorative elements unless they are visible in the screenshot.
Avoid remote dependencies that can cause render delays.
